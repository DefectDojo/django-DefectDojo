name: "release-X: Release docker containers"

env:
  GIT_USERNAME: "DefectDojo release bot"
  GIT_EMAIL: "dojo-release-bot@users.noreply.github.com"

on:
  workflow_dispatch:
    inputs:
      # the actual branch that can be chosen on the UI is made irrelevant by further steps
      # because someone will forget one day to change it.
      release_number:
        type: string
        description: 'Release version (x.y.z format)'
        required: true
      platform:
        type: string
        default: "linux/amd64"
  workflow_call:
    inputs:
      # the actual branch that can be chosen on the UI is made irrelevant by further steps
      # because someone will forget one day to change it.
      release_number:
        type: string
        description: 'Release version (x.y.z format)'
        required: true
      platform:
        type: string
        default: "linux/amd64"

jobs:
  job-build-and-push:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
          docker-image: [django, nginx]
          os: [alpine, debian]
    steps:

      - name: Prepare
        run: |
          platform=${{ inputs.platform }}
          echo "PLATFORM_PAIR=${platform//\//-}" >> $GITHUB_ENV

      - name: Login to DockerHub
        uses: docker/login-action@9780b0c442fbb1117ed29e0efdff1e18412f7567 # v3.3.0
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Checkout tag
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
        #   ref: ${{ github.event.inputs.release_number }}
          ref: arm64-unit-tests

      - id: set-repo-org
        run: echo "repoorg=$(echo ${GITHUB_REPOSITORY%%/*} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@b5ca514318bd6ebac0fb2aedd5d36ec1b5c232a2 # v3.10.0

#TODO : build once, retag for debian via https://github.com/koraykoska/retag-docker-image

      - name: Build and push images with debian
        id: 'build-debian'
        if: ${{ matrix.os  == 'debian' }}
        uses: docker/build-push-action@471d1dc4e07e5cdedd4c2171150001c434f0b7a4 # v6.15.0
        env:
          DOCKER_BUILD_CHECKS_ANNOTATIONS: false
          REPO_ORG: ${{ env.repoorg }}
          docker-image: ${{ matrix.docker-image }}
        with:
          push: true
        #   tags: ${{ env.REPO_ORG }}/defectdojo-${{ env.docker-image}}:${{ github.event.inputs.release_number }}-${{ matrix.os }}, ${{ env.REPO_ORG }}/defectdojo-${{ env.docker-image}}:${{ github.event.inputs.release_number }}, ${{ env.REPO_ORG }}/defectdojo-${{ env.docker-image}}:latest
          file: ./Dockerfile.${{ env.docker-image }}-${{ matrix.os }}
          context: .
          outputs: type=image,"name=${{ env.REPO_ORG }}/defectdojo-${{ env.docker-image}}-${{ matrix.os }}",push-by-digest=true,name-canonical=true

      - name: Export digest
        if: ${{ matrix.os  == 'debian' }}
        run: |
          mkdir -p ${{ runner.temp }}/digests
          digest="${{ steps.build-debian.outputs.digest }}"
          touch "${{ runner.temp }}/digests/${digest#sha256:}"

      - name: Upload digest
        if: ${{ matrix.os  == 'debian' }}
        uses: actions/upload-artifact@v4
        with:
          name: digests-${{ env.docker-image}}-${{ matrix.os }}-${{ env.PLATFORM_PAIR }}
          path: ${{ runner.temp }}/digests/*
          if-no-files-found: error
          retention-days: 1


      - name: Build and push images with alpine
        id: 'build-alpine'
        if: ${{ matrix.os  == 'alpine' }}
        uses: docker/build-push-action@471d1dc4e07e5cdedd4c2171150001c434f0b7a4 # v6.15.0
        env:
          DOCKER_BUILD_CHECKS_ANNOTATIONS: false
          REPO_ORG: ${{ env.repoorg }}
          docker-image: ${{ matrix.docker-image }}
        with:
          push: true
        #https://github.com/docker/build-push-action/discussions/1213
#https://github.com/erhhung/al2023-devops/blob/main/.github/workflows/ci.yml
#https://github.com/docker/buildx/issues/805
        #   tags: ${{ env.REPO_ORG }}/defectdojo-${{ env.docker-image}}:${{ github.event.inputs.release_number }}-${{ matrix.os }}
          file: ./Dockerfile.${{ env.docker-image }}-${{ matrix.os }}
          context: .
          outputs: type=image,"name=${{ env.REPO_ORG }}/defectdojo-${{ env.docker-image}}-${{ matrix.os }}",push-by-digest=true,name-canonical=true

      - name: Export digest
        if: ${{ matrix.os  == 'alpine' }}
        run: |
          mkdir -p ${{ runner.temp }}/digests
          digest="${{ steps.build-alpine.outputs.digest }}"
          touch "${{ runner.temp }}/digests/${digest#sha256:}"

      - name: Upload digest
        if: ${{ matrix.os  == 'alpine' }}
        uses: actions/upload-artifact@v4
        with:
          name: digests-${{ env.docker-image}}-${{ matrix.os }}-${{ env.PLATFORM_PAIR }}
          path: ${{ runner.temp }}/digests/*
          if-no-files-found: error
          retention-days: 1