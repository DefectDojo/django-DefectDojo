name: Merge back to master PR

env:
  GIT_USERNAME: "DefectDojo release bot"
  GIT_EMAIL: "dojo-release-bot@users.noreply.github.com"
on:
  workflow_dispatch:
    inputs:
      release_number:
        description: 'Future dev release version (x.y.z format). Will automatically be x.y.z-dev'
        required: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout dev branch
        uses: actions/checkout@v2
        with:
          ref: dev
      - name: Configure git
        run: |
          git config --global user.name "${{ env.GIT_USERNAME }}"
          git config --global user.email "${{ env.GIT_EMAIL }}"
      - name: Update version numbers in key files
        run: |
          sed -ri "s/__version__ = '.*'/__version__ = '${{ github.event.inputs.release_number }}-dev'/" dojo/__init__.py
          sed -ri "s/version='.*'/version='${{ github.event.inputs.release_number }}-dev'/" setup.py
          sed -ri "s/appVersion: \"([0-9]+(\.[0-9]+)*)\"/appVersion: \"${{ github.event.inputs.release_number }}-dev\"/" helm/defectdojo/Chart.yaml
      - name: Check numbers
        run: |
          grep version dojo/__init__.py
          grep version setup.py
          grep appVersion helm/defectdojo/Chart.yaml
      - name: Push version changes
        uses: stefanzweifel/git-auto-commit-action@v4.1.2
        with:
          commit_user_name: "${{ env.GIT_USERNAME }}"
          commit_user_email: "${{ env.GIT_EMAIL }}"
          commit_author: "${{ env.GIT_USERNAME }} <${{ env.GIT_EMAIL }}>"
          commit_message: "Update versions in application files"
          branch: ${{ github.head_ref }}
      - id: set-repo-org
        run: echo ::set-output name=repoorg::${GITHUB_REPOSITORY%%/*}
      - name: Create Pull Request
        env:
          REPO_ORG: ${{ steps.set-repo-org.outputs.repoorg }}
        uses: actions/github-script@v3
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            github.pulls.create({
              owner: ${{ env.REPO_ORG }}
              repo: 'django-DefectDojo',
              title: 'Post-release PR - master back to dev',
              body: `Release triggered by \`${ process.env.GITHUB_ACTOR }\``,
              head: 'master',
              base: 'dev'
            })
